"use client";
import { IPageError } from "@/interfaces/page-error/page-error";

export default function AboutError({ error, reset }: IPageError) {
  return <div className="flex flex-col items-center"></div>;
}
